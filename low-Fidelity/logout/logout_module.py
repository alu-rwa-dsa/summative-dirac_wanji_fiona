import sys
import time

def logout(obj):
    """cehck updates"""
    """logout the user"""
    """return to the main page"""
    time.sleep(1)
    #resave users progress
    #exit the processus
    time.sleep(1)
    sys.exit()
